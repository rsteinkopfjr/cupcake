<h1>Cupcakes</h1>

<h2>Cupcakes that are to be eaten!</h2>

<ul>
    {{#each cupcakes}}
        {{#unless devoured}}
            {{> cupcakes/cupcake-block devour=true}}
        {{/unless}}
    {{/each}}
</ul>

<h2>Cupcakes that have been devoured!</h2>

<ul>
    {{#each cupcakes}}
        {{#if devoured}}
            {{> cupcakes/cupcake-block devour=false}}
        {{/if}}
    {{/each}}
</ul>

<h2>Add a Cupcake</h2>
<form class="create-form">
    <div class="form-group">
        <label for="ca">Cat Name:</label>
        <input type="text" id="ca" name="cupcake_name">
    </div>
    <button type="submit">Add Cupcake</button>
</form>