<h1>Cupcakes!</h1>

<h2>Cupcakes that are not eaten!</h2>

<ul>
  {{#each cupcakes}}
    {{#unless devoured}}
      {{> cupcakes/cupcake-block devour=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Cupcakes that have been Devoured!</h2>

<ul>
  {{#each cupcakes}}
    {{#if devoured}}
      {{> cupcakes/cupcake-block devour=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Cupcake</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Cupcake Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">Devoured?</label><br>
    <input type="radio" name="devoured" value="1" checked> Devoured!<br>
    <input type="radio" name="devoured" value="0"> Not Eaten
  </div>

  <button type="submit">Add Cupcake</button>
</form>
